node_modules
# Keep environment variables out of version control


/src/generated/prisma

certificates
# SSL certificates (keep directory but ignore cert files)
certs/*.crt
certs/*.key
!certs/.gitkeep